import 'package:dart_questao02/dart_questao02.dart';
import 'package:test/test.dart';

void main() {
  test('calculate to 20', () {
    expect(calculateFunction(0), 100);
  });
  test('calculate to 0', () {
    expect(calculateFunction(0), 100);
  });
}
